# 项目开发规范

## Git 提交规则
- **只有在用户明确指令时**才进行 `git commit` 和 `git push`
- 不要自动提交代码
- message为英文

## 开发流程规范
- **开发功能前先不着急动代码**
- 先明确需求
- 再思考可能的实现方案
- 等待用户确认后再进行代码修改

## 国际化规范
- 所有用户可见的文字都需要国际化
- 使用 `addon/locale/` 目录下的 `.ftl` 文件管理字符串
- 通过 `getString()` 函数获取本地化字符串

## 代码规范
- 使用英文注释
- 保持代码简洁，去除冗余代码和调试日志
